#selectList=SELECT B.BID, B.BTYPE, B.BNO, C.CNAME, B.BTITLE, B.BCONTENT, M.NICK_NAME, B.BCOUNT, B.REF_BID, B.REF_REF_BID, B.REPLY_LEVEL, B.BDATE, B.STATUS FROM BOARD B JOIN MEMBER M ON (B.BWRITER = M.UNO) JOIN CATEGORY C ON (C.CID = B.CID) WHERE B.STATUS = 'Y' AND B.BTYPE = 1 AND B.REPLY_LEVEL = 0 ORDER BY B.BNO DESC
insertBoard=INSERT INTO BOARD VALUES (SEQ_BID.NEXTVAL, 1, SEQ_BNO1.NEXTVAL, ?, ?, ?, ?, DEFAULT, NULL, NULL, 0, SYSDATE, 'Y')
listCount=SELECT COUNT(*) FROM BOARD WHERE BTYPE='1'
selectList=SELECT RNUM, BID, BTYPE, BNO, CNAME, BTITLE, BCONTENT, NICK_NAME, BCOUNT, REF_BID, REF_REF_BID, REPLY_LEVEL, BDATE, STATUS FROM(SELECT ROWNUM RNUM, BID, BTYPE, BNO, CNAME, BTITLE, BCONTENT, NICK_NAME, BCOUNT, REF_BID, REF_REF_BID, REPLY_LEVEL, BDATE, STATUS FROM (SELECT B.BID, B.BTYPE, B.BNO, C.CNAME, B.BTITLE, B.BCONTENT, M.NICK_NAME, B.BCOUNT, B.REF_BID, B.REF_REF_BID, B.REPLY_LEVEL, B.BDATE, B.STATUS FROM BOARD B JOIN MEMBER M ON(B.BWRITER = M.UNO) JOIN CATEGORY C ON (C.CID = B.CID) WHERE B.STATUS = 'Y' AND B.BTYPE = '1' AND B.REPLY_LEVEL = 0 ORDER BY BID DESC)) WHERE RNUM BETWEEN ? AND ?
updateCount=UPDATE BOARD SET BCOUNT=(SELECT BCOUNT FROM BOARD WHERE BID=?) + 1 WHERE BID=?
selectOne=SELECT B.BID, B.BTYPE, B.BNO, C.CNAME, B.BTITLE, B.BCONTENT, M.NICK_NAME, B.BCOUNT, B.REF_BID, B.REF_REF_BID, B.REPLY_LEVEL, B.BDATE, B.STATUS FROM BOARD B JOIN MEMBER M ON(B.BWRITER=M.UNO) JOIN CATEGORY C ON (C.CID = B.CID) WHERE BID=?
insertThumb=INSERT INTO BOARD VALUES (SEQ_BID.NEXTVAL, 2, SEQ_BNO2.NEXTVAL, NULL, ?, ?, ?, DEFAULT, NULL, NULL, 0, SYSDATE, DEFAULT)
selectCurrval=SELECT SEQ_BID.CURRVAL FROM DUAL
insertAttachment=INSERT INTO ATTACHMENT VALUES (SEQ_FID.NEXTVAL, ?, ?, ?, ?, SYSDATE, ?, DEFAULT, DEFAULT)
selectThumbnailMap=SELECT B.BID, B.BNO, B.BTITLE, B.BCONTENT, M.NICK_NAME, B.BCOUNT, B.BDATE, A.FID, A.ORIGIN_NAME, A.CHANGE_NAME, A.FILE_PATH, A.UPLOAD_DATE FROM BOARD B JOIN ATTACHMENT A ON (B.BID = A.BID) JOIN MEMBER M ON (B.BWRITER = M.UNO) WHERE B.STATUS = 'Y' AND A.STATUS = 'Y' AND FILE_LEVEL = 0 ORDER BY 1 DESC
selectThumbnailOne=SELECT B.BID, B.BNO, B.BTITLE, B.BCONTENT, M.NICK_NAME, B.BCOUNT, B.BDATE, A.FID, A.ORIGIN_NAME, A.CHANGE_NAME, A.FILE_PATH, A.UPLOAD_DATE FROM BOARD B JOIN ATTACHMENT A ON (B.BID = A.BID) JOIN MEMBER M ON (B.BWRITER = M.UNO) WHERE B.STATUS = 'Y' AND A.STATUS = 'Y' AND A.BID = ?
selectOneAttachment=SELECT FID, BID, ORIGIN_NAME, CHANGE_NAME, FILE_PATH, UPLOAD_DATE, FILE_LEVEL, STATUS FROM ATTACHMENT WHERE FID = ?







